#!/bin/sh
#argshort.sh: Shows how one line if can be converted to a single line statement

[ $# -ne 3 ] && { echo "You have not entered 3 arguments";exit 3; }
echo "Am still alive, if you can see this, () was used in the exit statment"
